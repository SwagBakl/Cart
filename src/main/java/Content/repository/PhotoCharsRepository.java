package Content.repository;

import Content.entity.PhotoChars;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PhotoCharsRepository extends JpaRepository<PhotoChars, Long> {
}
