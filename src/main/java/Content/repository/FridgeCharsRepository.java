package Content.repository;

import Content.entity.FridgeChars;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FridgeCharsRepository extends JpaRepository<FridgeChars, Long> {
}
