package Content.repository;

import Content.entity.TabChars;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TabCharsRepository extends JpaRepository<TabChars, Long> {
}
