package Content.repository;

import Content.entity.WasherChars;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WasherCharsRepository extends JpaRepository<WasherChars, Long> {
}
